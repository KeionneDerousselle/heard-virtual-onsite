# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.
# API
/api/node_modules
/api/.build/**
/api/.webpack/**
/api/.esbuild
/api/coverage
/api/.serverless
/api/.eslintcache
/api/.env
/api/.env.development
/api/.env.test
# we're using yarn, adding this just in case someone runs npm instead of yarn
/api/package-lock.json

# dependencies
/node_modules
/.pnp
.pnp.js

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# local env files
.env*.local

# vercel
.vercel

# typescript
*.tsbuildinfo

# we're using yarn, adding this just in case someone runs npm instead of yarn
package-lock.json